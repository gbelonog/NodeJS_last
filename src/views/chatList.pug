doctype html
html(lang='en')
include _partials/head.pug
+head('ChatList')
body
    include _partials/header.pug
    h1 List of chats
    
    if login === 'true' 
        p= login    
    ul
        each val in items
            li= val 
            if login  === 'true'
                a(href='/chat') chat
    include _partials/footer.pug



   
